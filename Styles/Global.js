import { StyleSheet } from "react-native"

export const colors = {
    darkBlue: '#000',
    light: 'blue',
    brown: '#fff',
    lightBrown: '#000'
}

export default StyleSheet.create({
    container: {
        padding: 10,
        backgroundColor: colors.light
    },
    
})